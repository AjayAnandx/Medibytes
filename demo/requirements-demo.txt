# MediBytes investor demo — CPU-only
# Install: pip install -r requirements-demo.txt   (Python 3.13 Windows-safe)
imageio-ffmpeg==0.6.0
soundfile==0.13.1
numpy
faster-whisper==1.2.1
streamlit==1.40.0
matplotlib==3.9.2
jinja2==3.1.4
python-docx==1.1.2
# ── OPTIONAL (code auto-skips if absent) ──
# Denoise: needs scipy+noisereduce (noisereduce pulls librosa, which has NO
# Python 3.13 Windows wheel — install only on Python <=3.12):
# scipy==1.14.1
# noisereduce==3.0.2
# langdetect / indic-transliteration / pint are RESERVED for a future Stage-3
# upgrade — no demo code imports them yet, so they stay optional:
# langdetect==1.0.9
# indic-transliteration==2.3.68
# pint==0.24.3
# spacy is OPTIONAL (code skips it if absent; spacy 3.8.x has no Python 3.13 wheel).
# Only install on Python <=3.12 if you want the NER confidence nudge:
# pip install "spacy>=3.8.7" && python -m spacy download en_core_web_sm
streamlit==1.40.0
matplotlib==3.9.2
jinja2==3.1.4
python-docx==1.1.2
# After install (optional, Python <=3.12 only):
# python -m spacy download en_core_web_sm
# ollama pull qwen2.5:0.5b   (already on demo laptop)
